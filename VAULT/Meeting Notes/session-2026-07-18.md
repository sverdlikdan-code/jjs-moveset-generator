# Session 2026-07-18 #security

## Сделано

- **API token auth**: requireToken middleware на всех /api роутах (кроме /api/auth)
- **handleUnauth()**: 401 → чистим localStorage → редирект на / (старые сессии без токена)
- **Rate limiting**: generalLimiter 120/15min, writeLimiter 10/15min, authLimiter 10/15min
- **Helmet.js**: CSP, X-Frame-Options, X-Content-Type-Options; trust proxy 1 для Railway
- **Input validation**: json limit 100kb, zstd bomb check (>100k chars / >65k bytes), max 50 moves, sort whitelist
- **XSS fix**: escHtml() экранирует и кавычки; ALLOWED_TYPES whitelist в renderSlotCards
- **Decode panel**: переделан с мелких строк на anime live-cards (тот же CSS что live preview)
- **Footer disclaimer**: "Unofficial fan tool · Not affiliated with Shueisha, Roblox or game developers"
- **CRIT-02 Supabase RLS**: политики пересозданы с `TO service_role` — anon-key заблокирован
- getAuthHeaders() + все 8 fetch-вызовов в library.html обновлены

## Security статус

- ✅ Все CRIT + HIGH findings закрыты
- ⏳ MED-01..04 отложены на после беты

**Статус: security hardening завершён ✅**
